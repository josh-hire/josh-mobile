import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    gap: 8,
    marginTop: "20%",
  },
  containerFooter: {
    marginBottom: 36,
  },
});

export default styles;
