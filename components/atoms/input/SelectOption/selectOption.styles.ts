import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexWrap: "wrap",
    gap: 12
  },
  optionContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 8,
  },
});

export default styles;
