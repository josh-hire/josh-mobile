import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  secondaryButton: {
    borderRadius: 8,
    borderWidth: 1,
    padding: 12,
    gap: 12,
    flexDirection: "row",
  },
});

export default styles;